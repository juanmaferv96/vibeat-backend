package com.vibeat.backend.test;
/*
import com.theeasiestticket.backend.model.Usuario;
import com.theeasiestticket.backend.service.UsuarioService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

import java.time.LocalDate;

@SpringBootApplication
public class TestUsuarioCreation implements CommandLineRunner {

    @Autowired
    private UsuarioService usuarioService;

    public static void main(String[] args) {
        SpringApplication.run(TestUsuarioCreation.class, args);
    }

    @Override
    public void run(String... args) throws Exception {
        Usuario usuario = new Usuario();
        usuario.setNombre("Juan Manuel");
        usuario.setApellidos("Fernandez Velarde");
        usuario.setDni("48124004T");
        usuario.setFechaNacimiento(LocalDate.of(1996, 1, 24));
        usuario.setDireccion("Calle Cataluña, 22");
        usuario.setTelefono("659086000");
        usuario.setEmail("juanma.campanero96@gmail.com");
        usuario.setUser("juanmaferv96");
        usuario.setPassword("Campanero24196+");

        Usuario usuarioCreado = usuarioService.saveUsuario(usuario);
        System.out.println("Usuario creado con ID: " + usuarioCreado.getId());
    }
    
   
}
 */